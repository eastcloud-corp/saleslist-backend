services:
  - type: web
    name: saleslist-mock-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: ./
    rootDir: deployment/swagger
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /companies
    region: singapore